{
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write --ignore-unknown ."
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.3",
    "prettier": "^3.0.0"
  },
  "lint-staged": {
    "*": "prettier --write --ignore-unknown"
  }
}
